name: abaya_designer
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  video_player: ^2.8.2
  audioplayers: ^5.2.1
  carousel_slider: ^5.1.2 
  google_generative_ai: ^0.4.7 # Stable version for your setup
  firebase_core: ^3.1.1  # Or latest
  firebase_auth: ^5.1.1  # Or latest
  cloud_firestore: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/login.png
    - assets/images/splash.png
    - assets/images/bgdesign.jpg
    - assets/images/dummy1.png
